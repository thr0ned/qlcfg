unbindall
bind ~ "toggleconsole"

//binds
bind E +forward
bind S +moveleft
bind D +back
bind F +moveright

bind MOUSE2 +moveup
bind A +movedown
bind G +rhythm

bind MOUSE1 "+MOUSE1"

bind SPACE rl
bind CTRL +gl
bind B pg; bind N pg; bind M pg; bind Z pg
bind SHIFT +zoom; bind Q gl
bind V sg
bind R lg; bind W lg
bind MOUSE3 mg
bind MOUSE4 gt; bind MOUSE5 gt
bind MWHEELUP rg; bind MWHEELDOWN rg

alias +overlay "cg_drawTeamOverlay 2"
alias -overlay "cg_drawTeamOverlay 0"

bind C "togglechathistory"
bind ALT "+scores"
bind ENTER "messagemode"
bind \ "messagemode2"
bind ESCAPE +zoom
bind TAB "+mod"
bind X "+overlay"

bind 1 vote yes
bind 2 vote no
bind 3 readyup
// bind 4 pg
bind 5 "happyface"
