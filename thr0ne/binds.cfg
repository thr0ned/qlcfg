unbindall
bind ~ "toggleconsole"

// bind 7

//binds
bind E +forward
bind S +moveleft
bind D +back
bind F +moveright

BIND MOUSE2 +moveup
bind A +movedown
bind G +rhythm

bind MOUSE1 "+MOUSE1"
bind SHIFT +zoom

// WEAPON BINDS
bind SPACE rl
bind Z gl
bind X sg; bind V sg; bind C sg
bind 4 pg; bind 2 pg; bind 3 pg; bind 5 pg; bind 6 pg; bind T pg
bind R lg; bind W lg
bind MOUSE3 mg
bind MOUSE4 gt
bind MOUSE5 gt
bind MWHEELUP rg
bind MWHEELDOWN rg

alias +overlay "cg_drawTeamOverlay 2"
alias -overlay "cg_drawTeamOverlay 0"

bind H "togglechathistory"
bind CTRL "+chat"
bind ALT "+scores"
bind ENTER "messagemode"
bind \ "messagemode2"
bind ESCAPE +zoom

bind TAB "+mod"
bind N "+overlay"; bind B "+acc"

bind . vote yes
bind , vote no
bind 1 readyup
bind 7 "say ^3\( •ᴗ•)/"
